import type { IResolvers } from "@graphql-tools/utils";
import type { DocumentNode } from "graphql";
import type { Node } from "../classes";
import type Relationship from "../classes/Relationship";
import type { Neo4jFeaturesSettings, Neo4jGraphQLCallbacks } from "../types";
import type { Subgraph } from "../classes/Subgraph";
declare function makeAugmentedSchema(document: DocumentNode, { features, enableRegex, validateResolvers, generateSubscriptions, callbacks, userCustomResolvers, subgraph, }?: {
    features?: Neo4jFeaturesSettings;
    enableRegex?: boolean;
    validateResolvers?: boolean;
    generateSubscriptions?: boolean;
    callbacks?: Neo4jGraphQLCallbacks;
    userCustomResolvers?: IResolvers | Array<IResolvers>;
    subgraph?: Subgraph;
}): {
    nodes: Node[];
    relationships: Relationship[];
    typeDefs: DocumentNode;
    resolvers: IResolvers;
};
export default makeAugmentedSchema;
//# sourceMappingURL=make-augmented-schema.d.ts.map