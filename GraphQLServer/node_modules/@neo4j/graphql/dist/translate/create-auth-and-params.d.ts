import type { Node } from "../classes";
import type { BaseField, Context } from "../types";
import type { AuthOperations } from "../types/deprecated/auth/auth-operations";
import type { Rule } from "./deprecated/auth/types";
type AuthAndParams = {
    cypher: string;
    params: Record<string, any>;
};
export declare function createAuthAndParams({ entity, operations, skipRoles, skipIsAuthenticated, allow, context, bind, where, }: {
    entity: Node | BaseField;
    operations?: AuthOperations | AuthOperations[];
    skipRoles?: boolean;
    skipIsAuthenticated?: boolean;
    allow?: Rule;
    context: Context;
    bind?: Rule;
    where?: Rule;
}): AuthAndParams;
export {};
//# sourceMappingURL=create-auth-and-params.d.ts.map